# فاز دوم: پیشنهاد مسیر گوش دادن (Graph + Dijkstra)

## 📌 معرفی فاز دوم

**مسئله:**
پلتفرم EchoVerse برای کاربران می‌خواهد مسیرهای پیشنهادی گوش دادن بین اپیزودهای مختلف داشته باشد. هر اپیزود یک رأس (Vertex) و ارتباط بین دو اپیزود یک یال وزن‌دار است. وزن نشان‌دهندهٔ "هزینهٔ حرکت" یا "اختلاف موضوعی" است.

**راه‌حل:**
ساخت **گراف وزن‌دار بدون جهت** و استفاده از **الگوریتم Dijkstra** برای یافتن کوتاه‌ترین مسیرهای ممکن از یک اپیزود مبدأ به تمام اپیزودهای دیگر.

**چرا Dijkstra؟**
- کمترین هزینه را برای رفتن از مبدأ به هر مقصد می‌یابد
- برای گراف‌های وزن‌دار مناسب است (وزن‌ها مثبت)
- پیچیدگی O(V²) قابل قبول برای تعداد رأس‌های کم (26 حرف)

**قانون مهم:**
بدون استفاده از `PriorityQueue` یا دیگر کتابخانه‌های جاهز. تمام ساختارهای داده دستی هستند.

---

## 🎯 اهداف و الزامات

### اهداف فاز دوم

1. **ساخت گراف:** یک گراف وزن‌دار بدون جهت با آرایهٔ رأس‌ها و لیست‌های پیوندی یال‌ها
2. **درج یال:** اضافه کردن ارتباط‌های دوطرفه بین اپیزودها
3. **الگوریتم Dijkstra:** محاسبهٔ کوتاه‌ترین مسیرها از مبدأ
4. **نمایش نتایج:** چاپ مسیر و هزینهٔ رفتن به هر اپیزود

### الزامات

- بدون `ArrayList` یا `PriorityQueue`
- تمام ساختارهای داده دستی (آرایه، لیست پیوندی)
- الگوریتم Dijkstra صحیح (با مقدارهای اولیه، حلقهٔ اصلی، به‌روزرسانی)
- نمایش مسیر از مبدأ تا مقصد

---

## 📁 ساختار فایل‌ها

فایل‌های فاز دوم در پوشه `src/phase2_graph/` قرار دارند:

```text
src/phase2_graph/
    ├── GraphNode.java      (کلاس رأس)
    ├── Edge.java           (کلاس یال)
    ├── Graph.java          (کلاس گراف + Dijkstra)
    ├── Phase2Main.java     (برنامه اصلی)
    └── phase2.md           (این فایل)
```

---

## 💻 شرح فایل‌ها همراه با توضیح کد

### 1️⃣ GraphNode.java - ساختار رأس

**نقش:** نمایندگی هر اپیزود (رأس) در گراف. هر رأس شناسه، لیست یال‌های متصل، و اطلاعات Dijkstra را نگهدارد.

**کد کامل:**

```java
public class GraphNode {
    int id;                 // شناسه رأس (0=A، 1=B، ...، 25=Z)
    Edge edgeList;          // اولین یال در لیست پیوندی
    boolean visited;        // آیا در الگوریتم Dijkstra پردازش شده؟
    int distance;           // کوتاه‌ترین فاصلهٔ تا این رأس از مبدأ
    GraphNode previous;     // رأس قبلی در مسیر کوتاه‌ترین
    
    public GraphNode(int id) {
        this.id = id;
        this.edgeList = null;
        this.visited = false;
        this.distance = Integer.MAX_VALUE;  // بی‌نهایت
        this.previous = null;
    }
}
```

**توضیح خط به خط:**
- `int id`: شناسهٔ عددی رأس (حرف → عدد: A=0, B=1, ..., Z=25)
- `Edge edgeList`: اشاره‌گر به اولین یال در لیست پیوندی یال‌های متصل به این رأس
- `boolean visited`: پرچم برای نشان‌دادن اینکه این رأس پردازش شده یا نه
- `int distance`: فاصلهٔ کوتاه‌ترین مسیر از رأس مبدأ تا این رأس
- `GraphNode previous`: اشاره‌گر به رأس قبلی در مسیر کوتاه‌ترین (برای بازسازی مسیر)
- سازنده: اولیه‌سازی: `distance = Integer.MAX_VALUE` (بی‌نهایت)، `visited = false`

---

### 2️⃣ Edge.java - ساختار یال

**نقش:** نمایندگی یک یال (ارتباط) بین دو رأس.

**کد کامل:**

```java
public class Edge {
    GraphNode to;     // رأس مقصد این یال
    int weight;       // وزن یال (هزینهٔ حرکت)
    Edge next;        // یال بعدی در لیست پیوندی
    
    public Edge(GraphNode to, int weight) {
        this.to = to;
        this.weight = weight;
        this.next = null;
    }
}
```

**توضیح خط به خط:**
- `GraphNode to`: اشاره‌گر به رأس مقصد این یال
- `int weight`: وزن یال (هزینهٔ حرکت از رأس فعلی به رأس مقصد)
- `Edge next`: اشاره‌گر به یال بعدی در لیست پیوندی (برای یال‌های بیشتری که از یک رأس خارج می‌شوند)
- سازنده: تنظیم `to` و `weight`، و `next = null`

---

### 3️⃣ Graph.java - منطق گراف

**نقش:** مدیریت رأس‌ها، یال‌ها و اجرای الگوریتم Dijkstra.

#### **الف) آرایهٔ رأس‌ها:**

```java
private GraphNode[] nodes;   // آرایهٔ 26 رأس (برای A..Z)
private int size;            // تعداد رأس‌های موجود

public Graph(int maxNodes) {
    nodes = new GraphNode[maxNodes];
    size = 0;
}
```

**توضیح:**
- `GraphNode[] nodes`: آرایهٔ ثابت برای نگهداری رأس‌ها (شاخص 0 = A, 1 = B, ...)
- `int size`: تعداد رأس‌های اضافه‌شده تا حالا
- سازنده: آرایه را ایجاد کن و `size = 0`

#### **ب) اضافه کردن رأس:**

```java
public void addVertex(int id) {
    nodes[size++] = new GraphNode(id);
}
```

**توضیح خط به خط:**
- `nodes[size++]`: رأس جدید را در موقعیت `size` قرار بده، سپس `size` را یک واحد زیاد کن
- `new GraphNode(id)`: رأس جدید ایجاد کن با شناسهٔ `id`

#### **ج) یافتن رأس با شناسهٔ خاص:**

```java
private GraphNode getNode(int id) {
    for (int i = 0; i < size; i++) {
        if (nodes[i].id == id)
            return nodes[i];
    }
    return null;
}
```

**توضیح خط به خط:**
- حلقه: تمام رأس‌های موجود را جستجو کن
- اگر `id` برابر شناسهٔ رأس: رأس را برگردان
- اگر پیدا نشد: `null` برگردان

#### **د) اضافه کردن یال:**

```java
public void addEdge(int from, int to, int weight) {
    GraphNode fromNode = getNode(from);   // خط 1: رأس مبدأ را پیدا کن
    GraphNode toNode = getNode(to);       // خط 2: رأس مقصد را پیدا کن
    
    if (fromNode == null || toNode == null)  // خط 3: اگر یکی null است
        return;                                // خط 4: برگردان (خطا)
    
    // **خط 5-7: یال از from به to**
    Edge edge1 = new Edge(toNode, weight);   // یال جدید
    edge1.next = fromNode.edgeList;           // در ابتدای لیست قرار بده
    fromNode.edgeList = edge1;                // این یال حالا اولین است
    
    // **خط 8-10: یال معکوس (از to به from) - چون بدون جهت است**
    Edge edge2 = new Edge(fromNode, weight);  // یال معکوس
    edge2.next = toNode.edgeList;             // در ابتدای لیست قرار بده
    toNode.edgeList = edge2;                  // این یال حالا اولین است
}
```

**توضیح خط به خط:**
1-2. دو رأس را پیدا کن
3-4. اگر یک یا هر دو `null` هستند (وجود ندارند)، خروج کن
5. یال جدید از `from` به `to` ایجاد کن
6. `next` یال جدید را به اولین یال موجود از `from` تعیین کن
7. اولین یال از `from` را یال جدید قرار بده (درج در ابتدا = O(1))
8-10. معکوس: برای گراف بدون جهت

**مثال:** اگر `addEdge(0, 1, 4)` (A-B وزن 4):
```
A.edgeList → [B وزن 4] → [...]
B.edgeList → [A وزن 4] → [...]
```

#### **ه) الگوریتم Dijkstra:**

```java
public void dijkstra(int sourceId) {
    GraphNode source = getNode(sourceId);  // خط 1: رأس مبدأ را پیدا کن
    if (source == null) return;             // خط 2: اگر وجود ندارد، خروج
    
    // **خط 3-7: اولیه‌سازی تمام رأس‌ها**
    for (int i = 0; i < size; i++) {
        nodes[i].distance = Integer.MAX_VALUE;  // خط 4: فاصله = بی‌نهایت
        nodes[i].visited = false;               // خط 5: هنوز پردازش نشده
        nodes[i].previous = null;               // خط 6: بدون پیشین
    }
    
    source.distance = 0;  // خط 7: فاصلهٔ مبدأ = 0
    
    // **خط 8-25: حلقهٔ اصلی الگوریتم**
    for (int i = 0; i < size; i++) {
        // **خط 9-10: رأسی با کمترین فاصلهٔ ناپردازش را انتخاب کن**
        GraphNode current = getMinDistanceNode();
        if (current == null)
            break;
        
        current.visited = true;  // خط 11: این رأس پردازش شد
        
        // **خط 12-24: تمام یال‌های متصل به رأس فعلی را بررسی کن**
        Edge edge = current.edgeList;  // خط 12: اولین یال
        while (edge != null) {  // خط 13: تا زمانی که یالی وجود دارد
            GraphNode neighbor = edge.to;  // خط 14: رأس همسایه
            
            if (!neighbor.visited) {  // خط 15: اگر هنوز پردازش نشده
                int newDist = current.distance + edge.weight;  // خط 16: فاصلهٔ جدید
                
                // **خط 17-18: اگر فاصلهٔ جدید کوچک‌تر باشد، به‌روزرسانی کن**
                if (newDist < neighbor.distance) {
                    neighbor.distance = newDist;
                    neighbor.previous = current;
                }
            }
            
            edge = edge.next;  // خط 19: یال بعدی
        }
    }
}
```

**توضیح خط به خط:**
1. رأس مبدأ را پیدا کن
2. اگر وجود ندارد، خروج
3-7. تمام رأس‌ها را اولیه‌سازی کن (فاصله = ∞، visited = false)، فقط مبدأ distance = 0
8. حلقهٔ اصلی: تا زمانی که رأس‌های فرقی داریم
9-10. رأسی با کمترین فاصلهٔ ناپردازش را انتخاب کن
11. این رأس را "پردازش‌شده" علامت‌گذاری کن
12-24. تمام یال‌های متصل به این رأس را بررسی کن:
   - اگر همسایه هنوز پردازش نشده:
   - فاصلهٔ جدید = فاصلهٔ فعلی + وزن یال
   - اگر کوچک‌تر از فاصلهٔ موجود: به‌روزرسانی کن و previous را تنظیم کن

**پیچیدگی:** O(V²) - برای هر رأس، تمام رأس‌های دیگر را جستجو می‌کنیم

#### **و) پیدا کردن رأس با کمترین فاصله:**

```java
private GraphNode getMinDistanceNode() {
    GraphNode minNode = null;        // خط 1: هنوز رأسی پیدا نشده
    int min = Integer.MAX_VALUE;     // خط 2: کمترین = بی‌نهایت
    
    // **خط 3-10: تمام رأس‌های ناپردازش را بررسی کن**
    for (int i = 0; i < size; i++) {
        if (!nodes[i].visited && nodes[i].distance < min) {  // خط 4: اگر ناپردازش و فاصله کمتر
            min = nodes[i].distance;   // خط 5: کمترین را به‌روزرسانی کن
            minNode = nodes[i];        // خط 6: رأس کمترین را نگه‌دار
        }
    }
    
    return minNode;  // خط 7: رأس با کمترین فاصله را برگردان
}
```

**توضیح خط به خط:**
- حلقه: تمام رأس‌های ناپردازش را جستجو کن
- اگر فاصلهٔ این رأس کمتر از `min` باشد: آن را به‌روزرسانی کن
- برگردان رأس با کمترین فاصله

#### **ز) نمایش مسیر:**

```java
public void printPath(int destId) {
    GraphNode dest = getNode(destId);  // خط 1: رأس مقصد را پیدا کن
    
    if (dest == null || dest.distance == Integer.MAX_VALUE) {  // خط 2: اگر وجود ندارد یا قابل دسترس نیست
        System.out.println("No path exists");  // خط 3: پیامی نشان بده
        return;
    }
    
    printPathRecursive(dest);  // خط 4: مسیر را از انتها به ابتدا نمایش بده
    System.out.println("\nCost: " + dest.distance);  // خط 5: هزینهٔ کل
}

private void printPathRecursive(GraphNode node) {
    if (node == null) return;  // خط 1: پایان بازگشت
    
    // **خط 2-4: اگر previous موجود است، ابتدا آن را نمایش بده**
    if (node.previous != null) {
        printPathRecursive(node.previous);
        System.out.print(" -> ");
    }
    
    // **خط 5: این رأس را نمایش بده**
    System.out.print((char)('A' + node.id));  // خط 5: تبدیل عدد به حرف (0→A, 1→B)
}
```

**توضیح خط به خط:**
- رأس مقصد را پیدا کن
- اگر وجود ندارد یا غیرقابل دسترس: پیامی نشان بده
- وگرنه: مسیر را نمایش بده (با فراخوانی `printPathRecursive`)
- `printPathRecursive` بازگشتی: اگر `previous` موجود است، ابتدا آن را نمایش بده
- سپس رأس فعلی را نمایش بده
- تبدیل id به حرف: `(char)('A' + id)` (0→'A', 1→'B', ...)

---

### 4️⃣ Phase2Main.java - واسط کاربر

**نقش:** دریافت دستورات از کاربر و مدیریت گراف.

**کد کامل:**

```java
import java.util.Scanner;

public class Phase2Main {
    public static void main(String[] args) {
        Graph graph = new Graph(26);         // خط 1: گراف برای 26 رأس
        Scanner sc = new Scanner(System.in); // خط 2: ورودی
        
        // **خط 3: حلقهٔ ورودی**
        while (sc.hasNext()) {
            String command = sc.next();  // خط 4: دستور را بخوان
            
            // **خط 5-12: دستور "insert" - اضافه کردن یال**
            if (command.equals("insert")) {
                String fromStr = sc.next();      // خط 6: اپیزود از (A-Z)
                String toStr = sc.next();        // خط 7: اپیزود تا (A-Z)
                int weight = sc.nextInt();       // خط 8: وزن یال
                
                int fromId = fromStr.charAt(0) - 'A';  // خط 9: تبدیل A→0, B→1, ...
                int toId = toStr.charAt(0) - 'A';      // خط 10: تبدیل
                
                graph.addVertex(fromId);   // خط 11: اضافه کن (اگر موجود نیست)
                graph.addVertex(toId);     // خط 12: اضافه کن (اگر موجود نیست)
                graph.addEdge(fromId, toId, weight);  // خط 13: یال بین رأس‌ها
            }
            // **خط 14-24: دستور "show" - اجرای Dijkstra و نمایش نتایج**
            else if (command.equals("show")) {
                String sourceStr = sc.next();     // خط 15: اپیزود مبدأ
                int sourceId = sourceStr.charAt(0) - 'A';  // خط 16: تبدیل
                
                System.out.println("Source Episode: " + sourceStr);  // خط 17: نمایش مبدأ
                
                graph.dijkstra(sourceId);  // خط 18: اجرای الگوریتم
                
                // **خط 19-24: نمایش مسیرهای تمام اپیزودها**
                for (int i = 0; i < 26; i++) {
                    System.out.println("\nEpisode " + (char)('A' + i) + ":");  // خط 20: نام اپیزود
                    System.out.print("Path: ");      // خط 21: پیشوند
                    graph.printPath(i);              // خط 22: نمایش مسیر و هزینه
                }
            }
        }
    }
}
```

**توضیح خط به خط:**
1. گراف ایجاد کن (برای 26 رأس A-Z)
2. ورودی را اولیه‌سازی کن
3. حلقهٔ ورودی: تا زمانی که دستور موجود است
4. دستور را بخوان
5-13. اگر دستور "insert" است:
   - سه آرگومان بخوان: از، تا، وزن
   - تبدیل حروف به اعداد
   - اگر وجود ندارند، رأس‌ها را اضافه کن
   - یال بین رأس‌ها را اضافه کن
14-24. اگر دستور "show" است:
   - مبدأ را بخوان
   - Dijkstra را اجرا کن
   - تمام اپیزودها را نمایش بده (با مسیر و هزینه)

---

## 🧪 نحوه اجرا و تست کردن

### **مرحلهٔ 1: کامپایل کردن**

در پوشهٔ `src` این دستور را اجرا کنید:

```bash
javac phase2_graph/*.java
```

### **مرحلهٔ 2: اجرا کردن**

```bash
java phase2_graph.Phase2Main
```

---

### **نمونهٔ تست ۱: گراف ساده**

**ورودی:**
```
insert A B 4
insert A C 2
insert B C 1
insert B D 5
insert C D 8
insert D E 2
show A
```

**خروجی مورد انتظار:**
```
Source Episode: A

Episode A:
Path: A
Cost: 0

Episode B:
Path: A -> C -> B
Cost: 3

Episode C:
Path: A -> C
Cost: 2

Episode D:
Path: A -> C -> B -> D
Cost: 8

Episode E:
Path: A -> C -> B -> D -> E
Cost: 10
```

**توضیح:**
- از A به A: 0 (خود)
- از A به B: A → C (2) → B (1) = 3 (بهتر از A → B مستقیم = 4)
- از A به C: A → C = 2
- از A به D: A → C → B → D = 8 (بهتر از A → C → D = 10)
- از A به E: A → C → B → D → E = 10 (بهتر از A → C → E = 12)

---

### **نمونهٔ تست ۲: مسیر غیرقابل دسترس**

**ورودی:**
```
insert A B 5
insert C D 3
show A
```

**خروجی مورد انتظار:**
```
Source Episode: A

Episode A:
Path: A
Cost: 0

Episode B:
Path: A -> B
Cost: 5

Episode C:
Path: No path exists

Episode D:
Path: No path exists
...
```

**توضیح:**
- C و D متصل به A نیستند
- "No path exists" برای اپیزودهای دسترس‌نشدنی

---

## ⚠️ نکات مهم

1. **لیست پیوندی:** یال‌ها در ابتدای لیست درج می‌شوند (O(1))
2. **بدون جهت:** هر یال دوطرفه است (از A به B و از B به A)
3. **Dijkstra:** یک رأس فقط یک بار پردازش می‌شود
4. **مسیر:** از `previous` برگردان برای بازسازی مسیر

---

## ✅ خلاصهٔ فاز دوم

- ✅ گراف وزن‌دار بدون جهت دستی پیاده‌سازی‌شده
- ✅ الگوریتم Dijkstra صحیح (O(V²))
- ✅ یال‌ها در لیست‌های پیوندی نگهداری می‌شوند
- ✅ مسیرها به‌صورت صحیح بازسازی و نمایش می‌شوند
- ✅ بدون کتابخانه‌های آماده
