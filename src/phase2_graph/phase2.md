
# فاز دوم پروژه ساختمان داده  

## پیشنهاد مسیر گوش دادن بین اپیزودها (Graph + Dijkstra)

---

## 🎯 هدف فاز دوم

در این فاز هدف پیاده‌سازی سیستمی برای **پیشنهاد مسیر گوش دادن بین اپیزودهای پادکست** است.  
هر اپیزود به عنوان یک رأس (Vertex) در گراف در نظر گرفته می‌شود و ارتباط بین اپیزودها با یال‌های وزن‌دار نمایش داده می‌شود.

وزن یال‌ها نشان‌دهنده میزان شباهت موضوعی یا هزینه‌ی جابه‌جایی ذهنی بین دو اپیزود است.  
هدف نهایی، پیدا کردن **کوتاه‌ترین مسیر** از یک اپیزود مشخص به سایر اپیزودها با استفاده از الگوریتم **Dijkstra** می‌باشد.

---

## 📌 مشخصات گراف

- گراف **بدون جهت (Undirected)** است
- گراف **وزن‌دار** است
- استفاده از کتابخانه‌های آماده گراف ممنوع بوده
- پیاده‌سازی به صورت کاملاً دستی انجام شده است

---

## 🗂 ساختار فایل‌ها

### 📁 phase2_graph/

#### 1️⃣ `GraphNode.java`

نماینده‌ی هر رأس در گراف

ویژگی‌ها:

- `id` : شناسه رأس (عدد متناظر با حروف A, B, C, ...)
- `edgeList` : لیست یال‌های متصل به رأس
- `distance` : فاصله رأس از مبدأ (برای دایکسترا)
- `visited` : مشخص می‌کند رأس پردازش شده یا نه
- `previous` : رأس قبلی در کوتاه‌ترین مسیر

---

#### 2️⃣ `Edge.java`

نماینده‌ی یال بین دو رأس

ویژگی‌ها:

- `to` : رأس مقصد
- `weight` : وزن یال
- `next` : اشاره‌گر به یال بعدی (لیست پیوندی)

---

#### 3️⃣ `Graph.java`

هسته‌ی اصلی گراف و الگوریتم دایکسترا

وظایف اصلی:

- اضافه کردن رأس‌ها
- اضافه کردن یال‌ها
- اجرای الگوریتم Dijkstra
- چاپ مسیر و هزینه کوتاه‌ترین مسیر

توابع مهم:

- `addVertex(int id)`
- `addEdge(int from, int to, int weight)`
- `dijkstra(int sourceId)`
- `printPath(int destId)`

---

#### 4️⃣ `Phase2Main.java`

فایل اصلی اجرای برنامه

وظایف:

- دریافت ورودی‌ها از کاربر
- ساخت گراف بر اساس دستورات `insert`
- اجرای الگوریتم دایکسترا با دستور `show`
- نمایش خروجی مطابق فرمت پروژه

---

## 🔁 فرمت ورودی

### اضافه کردن یال

```

insert A B 4

```

### اجرای دایکسترا

```

show A

```

---

## 📤 فرمت خروجی

نمونه خروجی:

```

Source Episode: A

Episode B:
Path: A -> C -> B
Cost: 3

```

- مسیر به صورت حروف نمایش داده می‌شود
- رأس‌ها با `->` به هم متصل هستند
- هزینه مسیر در خط جداگانه چاپ می‌شود

---

## 🧠 الگوریتم Dijkstra

در این پروژه:

- فاصله اولیه همه رأس‌ها برابر `∞` در نظر گرفته شده
- فاصله مبدأ برابر `0` است
- در هر مرحله رأسی با کمترین فاصله انتخاب می‌شود
- فاصله همسایه‌ها در صورت کوتاه‌تر شدن به‌روزرسانی می‌شود
- مسیر نهایی با استفاده از فیلد `previous` بازسازی می‌شود

---

## ✅ نتیجه‌گیری فاز دوم

در این فاز:

- گراف وزن‌دار بدون جهت به صورت دستی پیاده‌سازی شد
- الگوریتم دایکسترا بدون استفاده از کتابخانه آماده اجرا شد
- خروجی برنامه دقیقاً مطابق نمونه پروژه تولید شد
- کد ساده، قابل فهم و در سطح دانشجویی نوشته شده است

---
