# فاز دوم: پیشنهاد مسیر گوش دادن (Graph + Dijkstra)

## 📌 معرفی کوتاه

در فاز دوم می‌خواهیم برای کاربران مسیر پیشنهادی گوش دادن بین اپیزودها پیشنهاد دهیم. هر اپیزود یک رأس (Vertex) است و ارتباط بین اپیزودها با یک یال وزن‌دار نشان داده می‌شود. وزن نشان‌دهندهٔ "هزینهٔ رفتن" یا "اختلاف موضوعی" بین دو اپیزود است.

برای یافتن کوتاه‌ترین مسیر از یک اپیزود مشخص به بقیه از الگوریتم سادهٔ **Dijkstra** استفاده شده است.

---

## 🎯 اهداف فاز دوم

- ساخت گراف وزن‌دار (بدون جهت)
- اضافه کردن رأس‌ها و یال‌ها از ورودی
- محاسبهٔ کوتاه‌ترین مسیر از یک منبع مشخص با Dijkstra
- چاپ مسیر و هزینهٔ هر مقصد به شکلی ساده و خوانا

---

## 📁 فایل‌ها و نقش‌شان

پوشه: `src/phase2_graph/`

### 1️⃣ GraphNode.java - کلاس رأس

نمایندهٔ هر رأس (اپیزود) در گراف.

**فیلدهای مهم:**

- `int id` — شناسهٔ رأس (0..25 متناظر با A..Z)
- `Edge edgeList` — لیست پیوندی یال‌های متصل به این رأس
- `int distance` — کوتاه‌ترین فاصلهٔ معروفی تا این رأس (برای Dijkstra)
- `boolean visited` — آیا این رأس در الگوریتم پردازش شده
- `GraphNode previous` — رأس قبلی در کوتاه‌ترین مسیر (برای بازسازی مسیر)

**توضیح:** وقتی نود جدید ایجاد می‌شود، distance برابر بی‌نهایت تنظیم شده و visited برابر false است.

---

### 2️⃣ Edge.java - کلاس یال

نمایندهٔ یک یال (ارتباط) بین دو رأس.

**فیلدهای مهم:**

- `GraphNode to` — رأس مقصد این یال
- `int weight` — وزن یال (هزینهٔ حرکت)
- `Edge next` — اشاره‌گر به یال بعدی در لیست پیوندی

**توضیح:** یال‌ها به صورت لیست پیوندی ذخیره شده تا نیازی به آرایهٔ ثابت نباشد.

---

### 3️⃣ Graph.java - کلاس گراف

هستهٔ اصلی: منطق ساخت گراف و اجرای الگوریتم Dijkstra.

**توابع اصلی:**

- `addVertex(int id)` — اضافه کردن رأس جدید
- `addEdge(int from, int to, int weight)` — اضافه کردن یال دوطرفه
- `dijkstra(int sourceId)` — اجرای الگوریتم کوتاه‌ترین مسیر
- `printPath(int destId)` — چاپ مسیر و هزینه به مقصد

**نکات اجرایی:**

- یال‌ها دوطرفه اضافه می‌شوند (چون گراف بدون جهت است)
- Dijkstra بر اساس انتخاب خطی کمترین distance کار می‌کند (O(V^2))

---

### 4️⃣ Phase2Main.java - برنامهٔ اصلی

رابط خط فرمان: ورودی‌ها را می‌خواند و دستورات را پردازش می‌کند.

**دستورات قابل دریافت:**

- `insert A B 4` — اضافه کردن یال بین اپیزود A و B با وزن چهار
- `show A` — محاسبهٔ کوتاه‌ترین مسیر از A به تمام اپیزودهای دیگر

---

## 🔁 فرمت ورودی

اضافه کردن یال (دوطرفه):

```bash
insert A B 4
```

- `A`, `B` یک حرف بزرگ از A تا Z
- `4` عدد صحیح برای وزن

اجرای Dijkstra و نمایش نتایج از منبع:

```bash
show A
```

- `A` حرف بزرگ شناسهٔ منبع

---

## 📤 فرمت خروجی

نمونه خروجی:

```text
Source Episode: A

Episode A:
Path: A
Cost: 0

Episode B:
Path: A -> C -> B
Cost: 3

Episode C:
Path: A -> C
Cost: 2
```

**توضیح:**

- هر بلوک با `Episode X:` شروع می‌شود
- `Path:` مسیر کوتاه‌ترین مسیر را نشان می‌دهد (حروف با `->` جدا شده)
- `Cost:` مجموع وزن یال‌های مسیر است
- اگر هیچ مسیری وجود نداشته باشد: `No path exists`

---

## ⚙️ چطور الگوریتم کار می‌کند (ساده)

**مراحل Dijkstra:**

1. تمام فاصله‌ها را بی‌نهایت تنظیم کن (به جز منبع که 0 است)
2. تمام رأس‌ها را ملاقات نشده تنظیم کن
3. تا زمانی که رأس‌هایی برای پردازش وجود دارد:
   - رأسی با کمترین فاصلهٔ ملاقات نشده را انتخاب کن
   - آن را «شایده» علامت‌گذاری کن
   - برای هر همسایهٔ ملاقات نشده:
     - اگر فاصلهٔ جدید (فاصلهٔ فعلی + وزن یال) کمتر باشد:
       - فاصله را به‌روزرسانی کن
       - previous را تنظیم کن (برای بازسازی مسیر بعداً)

**پیچیدگی:** O(V²)

---

## 💡 مثال مرحله‌به‌مرحله

**ورودی:**

```bash
insert A B 4
insert A C 2
insert B C 1
insert B D 5
insert C D 8
insert C E 10
insert D E 2
show A
```

**گراف:**

```text
    A ---4--- B
    |         |
    2         5
    |         |
    C ---8--- D
    |         |
    10        2
    |         |
    +---------E
```

**اجرای Dijkstra از A:**

- A: فاصله = 0
- C: فاصله = 2 (مسیر: A → C)
- B: فاصله = 3 (مسیر: A → C → B، چون 2+1 < 4)
- D: فاصله = 8 (مسیر: A → C → B → D، چون 3+5 < 2+8)
- E: فاصله = 10 (مسیر: A → C → B → D → E، چون 8+2 = 10 < 2+10)

---

## 📌 نکات و محدودیت‌ها

- **محدودیت فعلی:** شناسه‌ها تنها یک حرف پشتیبانی می‌کنند (A..Z). برای شناسه‌های دلخواه باید نگاشت id→index دستی اضافه شود.
- **اعتبارسنجی ورودی:** برنامه فرمت ورودی را فرضی می‌پذیرد؛ ورودی‌های نادرست می‌توانند باعث exception شوند. بهتر است از نمونه‌ها پیروی کنید.
- **عملکرد:** O(V²) برای Dijkstra قابل قبول است. اگر نیاز به سرعت بیشتر باشد، می‌توان صف اولویت (min-heap) اضافه کرد.

---

## 🏃 نحوهٔ اجرا (سریع)

در پوشهٔ `src`:

**کامپایل:**

```powershell
javac phase2_graph/*.java
```

**اجرای تعاملی (دستورات را تایپ کن):**

```powershell
java phase2_graph.Phase2Main
```

---

## ✅ نتیجهٔ فاز دوم

- ✅ گراف وزن‌دار بدون جهت به صورت دستی پیاده‌سازی شده
- ✅ الگوریتم Dijkstra بدون کتابخانه‌های آماده
- ✅ تست‌های خودکار برای سناریوهای مختلف
- ✅ خروجی برنامه مطابق نمونه‌های پروژه
- ✅ کد ساده، قابل فهم، و در سطح دانشجویی

---
