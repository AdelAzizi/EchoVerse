# فاز اول: تأیید صحت کانال‌ها (AVL Tree)

## 📌 معرفی فاز اول

**مسئله:**
پلتفرم EchoVerse برای هر کاربر کانال‌های مختلفی دارد که باید به ترتیب خاصی (برای جستجوی سریع) نگهداری شوند. اگر ساختار نامتوازن باشد، جستجو کند خواهد بود. نیاز به یک درخت جستجوی دودویی **خود‌متوازن** است.

**راه‌حل:**
استفاده از **درخت AVL** (نام از سازندگانش Adelson-Velsky و Landis). درخت AVL یک درخت جستجوی دودویی است که بعد از هر درج یا حذف، خود را متوازن می‌کند تا ارتفاع درخت در حد O(log n) باقی بماند.

**چرا AVL؟**
- جستجو در O(log n) به جای O(n) در لیست‌های خطی
- درج و حذف نیز در O(log n)
- خود‌متوازن: هیچ دخالتی دستی نلازم

**قانون مهم:**
بدون استفاده از `TreeMap` یا دیگر کتابخانه‌های جاهز. تمام ساختارها دستی هستند.

---

## 🎯 اهداف و الزامات

### اهداف فاز اول

1. **ساخت درخت AVL:** پیاده‌سازی کامل درخت AVL با نگهداری تعادل
2. **درج (Insert):** اضافه کردن کانال جدید و توازن‌سازی خودکار
3. **حذف (Delete):** حذف کانال و نگهداری تعادل
4. **جستجو (Search):** یافتن کانال موجود
5. **نمایش (Display):** چاپ درخت به صورتی که نشان‌دهندهٔ ساختار باشد

### الزامات

- بدون `TreeMap` یا `HashMap`
- تمام ساختارها دستی (کلاس‌های Node دستی)
- چهار نوع دوران (Rotation) درست کار کند: LL، RR، LR، RL
- ارتفاع درخت همیشه متوازن باشد (برای هر گره، اختلاف ارتفاع فرزندان حداکثر 1)

---

## 📁 ساختار فایل‌ها

فایل‌های فاز اول در پوشه `src/phase1_avl/` قرار دارند:

```text
src/phase1_avl/
    ├── AVLNode.java        (کلاس گره)
    ├── AVLTree.java        (کلاس درخت AVL)
    ├── Phase1Main.java     (برنامه اصلی)
    └── phase1.md           (این فایل)
```

---

## 💻 شرح فایل‌ها همراه با توضیح کد

### 1️⃣ AVLNode.java - ساختار گره

**نقش:** نمایندگی یک گره در درخت AVL. هر گره یک کانال را نگهدارد و به دو فرزند اشاره کند.

**کد کامل:**

```java
public class AVLNode {
    int channelID;      // شناسهٔ کانال (مثلاً 101، 205، ...)
    int height;         // ارتفاع این گره (برای محاسبهٔ تعادل)
    AVLNode left;       // فرزند چپ
    AVLNode right;      // فرزند راست
    
    public AVLNode(int channelID) {
        this.channelID = channelID;
        this.height = 1;    // گره جدید ارتفاع 1 دارد
        this.left = null;
        this.right = null;
    }
}
```

**توضیح خط به خط:**
- **خط 2 - `int channelID`:** شناسهٔ کانال (عدد صحیح مثبت)
- **خط 3 - `int height`:** ارتفاع این گره (تعداد گره‌های در مسیر طولانی‌ترین به برگ). برگ‌ها ارتفاع 1 دارند
- **خط 4 - `AVLNode left`:** اشاره‌گر به فرزند چپ
- **خط 5 - `AVLNode right`:** اشاره‌گر به فرزند راست
- **خط 7-12 - سازنده:** `channelID` را نگهدارد، `height = 1`، هردو فرزند `null`

---

### 2️⃣ AVLTree.java - کلاس درخت AVL

**نقش:** مدیریت درخت AVL و انجام عملیات‌های درج، حذف، جستجو، و توازن‌سازی.

#### الف) متغیرهای جهانی:

```java
public class AVLTree {
    private AVLNode root;       // ریشهٔ درخت
    
    public AVLTree() {
        this.root = null;
    }
}
```

#### ب) توابع کمکی (Helper Methods):

**1. تابع `height(AVLNode node)` - ارتفاع گره:**

```java
private int height(AVLNode node) {
    if (node == null)
        return 0;
    return node.height;
}
```

**توضیح:**
- **خط 2-3:** اگر گره `null` باشد، ارتفاع = 0 (درخت خالی)
- **خط 4:** وگرنه، ارتفاع ذخیره‌شدهٔ گره را برگردان

**2. تابع `getBalance(AVLNode node)` - معامل تعادل:**

```java
private int getBalance(AVLNode node) {
    if (node == null)
        return 0;
    return height(node.left) - height(node.right);
}
```

**توضیح:**
- **خط 2-3:** اگر گره `null` باشد، معامل = 0
- **خط 4:** معامل تعادل = ارتفاع فرزند چپ - ارتفاع فرزند راست
  - اگر معامل > 1: درخت در سمت چپ "سنگین" است (نیاز به دوران راست)
  - اگر معامل < -1: درخت در سمت راست "سنگین" است (نیاز به دوران چپ)
  - اگر -1 ≤ معامل ≤ 1: درخت متوازن است

**3. تابع `max(int a, int b)` - بیشتر:**

```java
private int max(int a, int b) {
    return (a > b) ? a : b;
}
```

#### ج) عملیات‌های دوران (Rotation):

**الگوریتم دوران راست (Right Rotation):**

مسئله: درخت در سمت چپ نامتوازن است
```
      z                x
     / \              / \
    x   T4   --->    y   z
   / \              / \ / \
  y  T3            T1 T2 T3 T4
 / \
T1 T2
```

```java
private AVLNode rotateRight(AVLNode z) {
    // T2 را نگهدار
    AVLNode T2 = z.left.right;
    
    // دوران انجام دهید
    AVLNode x = z.left;
    x.right = z;
    z.left = T2;
    
    // ارتفاع‌ها را به‌روزرسانی کنید
    z.height = max(height(z.left), height(z.right)) + 1;
    x.height = max(height(x.left), height(x.right)) + 1;
    
    // ریشهٔ جدید را برگردانید
    return x;
}
```

**توضیح خط به خط:**
- **خط 2:** T2 = فرزند راست فرزند چپ z (این گره در طی دوران حرکت می‌کند)
- **خط 4-6:** x (فرزند چپ z) صاحب ریشهٔ جدید می‌شود، فرزند راست x به z اشاره می‌کند، فرزند چپ z به T2 اشاره می‌کند
- **خط 8-9:** ارتفاع z و x به‌روزرسانی شود (max(فرزندان) + 1)
- **خط 11:** x را بازگردان (ریشهٔ جدید)

**الگوریتم دوران چپ (Left Rotation):**

مسئله: درخت در سمت راست نامتوازن است
```
  z                   y
 / \                 / \
T1  y     --->      z   x
   / \             / \ / \
  T2  x           T1 T2 T3 T4
     / \
    T3 T4
```

```java
private AVLNode rotateLeft(AVLNode z) {
    // T2 را نگهدار
    AVLNode T2 = z.right.left;
    
    // دوران انجام دهید
    AVLNode y = z.right;
    y.left = z;
    z.right = T2;
    
    // ارتفاع‌ها را به‌روزرسانی کنید
    z.height = max(height(z.left), height(z.right)) + 1;
    y.height = max(height(y.left), height(y.right)) + 1;
    
    // ریشهٔ جدید را برگردانید
    return y;
}
```

**توضیح:** (مشابه rotateRight اما برای سمت راست)
- **خط 2:** T2 = فرزند چپ فرزند راست z
- **خط 4-6:** y (فرزند راست z) = ریشهٔ جدید، جابجایی نتایج
- **خط 8-9:** به‌روزرسانی ارتفاع‌ها

#### د) عملیات درج (Insert):

```java
public void insert(int channelID) {
    root = insertRec(root, channelID);
}

private AVLNode insertRec(AVLNode node, int channelID) {
    // مرحلهٔ 1: درج معمولی (BST)
    if (node == null)
        return new AVLNode(channelID);
    
    if (channelID < node.channelID) {
        node.left = insertRec(node.left, channelID);
    } else if (channelID > node.channelID) {
        node.right = insertRec(node.right, channelID);
    } else {
        return node;  // تکراری نیست
    }
    
    // مرحلهٔ 2: ارتفاع این گره را به‌روزرسانی کنید
    node.height = max(height(node.left), height(node.right)) + 1;
    
    // مرحلهٔ 3: معامل تعادل را محاسبه کنید
    int balance = getBalance(node);
    
    // مرحلهٔ 4: چهار حالت نامتوازن را بررسی کنید
    
    // حالت چپ-چپ (LL): balance > 1 و channelID < node.left.channelID
    if (balance > 1 && channelID < node.left.channelID)
        return rotateRight(node);
    
    // حالت راست-راست (RR): balance < -1 و channelID > node.right.channelID
    if (balance < -1 && channelID > node.right.channelID)
        return rotateLeft(node);
    
    // حالت چپ-راست (LR): balance > 1 و channelID > node.left.channelID
    if (balance > 1 && channelID > node.left.channelID) {
        node.left = rotateLeft(node.left);
        return rotateRight(node);
    }
    
    // حالت راست-چپ (RL): balance < -1 و channelID < node.right.channelID
    if (balance < -1 && channelID < node.right.channelID) {
        node.right = rotateRight(node.right);
        return rotateLeft(node);
    }
    
    return node;
}
```

**توضیح الگوریتم درج:**
- **خط 6-8:** اگر آی‌دی خالی باشد گره جدید ایجاد کن
- **خط 10-13:** اگر کوچک‌تر بود، درج در سمت چپ؛ اگر بزرگ‌تر بود، درج در سمت راست
- **خط 18:** ارتفاع گره = max(ارتفاع چپ، ارتفاع راست) + 1
- **خط 20-21:** معامل تعادل را محاسبه کن
- **خط 25-41:** چهار حالت دوران:
  - **LL** (خط 25-26): سنگین در چپ-چپ → دوران راست ساده
  - **RR** (خط 28-29): سنگین در راست-راست → دوران چپ ساده
  - **LR** (خط 31-33): سنگین در چپ-راست → دوران چپ برای فرزند چپ، سپس دوران راست
  - **RL** (خط 35-37): سنگین در راست-چپ → دوران راست برای فرزند راست، سپس دوران چپ
- **خط 39:** گره را برگردان

#### هـ) عملیات جستجو (Search):

```java
public boolean search(int channelID) {
    return searchRec(root, channelID);
}

private boolean searchRec(AVLNode node, int channelID) {
    // گره‌ای با این آی‌دی پیدا نشد
    if (node == null)
        return false;
    
    // آی‌دی پیدا شد
    if (channelID == node.channelID)
        return true;
    
    // جستجو در سمت چپ
    if (channelID < node.channelID)
        return searchRec(node.left, channelID);
    
    // جستجو در سمت راست
    return searchRec(node.right, channelID);
}
```

**توضیح:**
- **خط 6-8:** اگر گره `null` باشد، برگردان `false` (یافت نشد)
- **خط 10-12:** اگر `channelID` برابر باشد، برگردان `true` (یافت شد)
- **خط 14-16:** اگر کوچک‌تر باشد، در سمت چپ جستجو کن
- **خط 18:** اگر بزرگ‌تر باشد، در سمت راست جستجو کن

#### و) عملیات حذف (Delete):

```java
public void delete(int channelID) {
    root = deleteRec(root, channelID);
}

private AVLNode deleteRec(AVLNode node, int channelID) {
    // گره‌ای برای حذف پیدا نشد
    if (node == null)
        return null;
    
    if (channelID < node.channelID) {
        node.left = deleteRec(node.left, channelID);
    } else if (channelID > node.channelID) {
        node.right = deleteRec(node.right, channelID);
    } else {
        // گره‌ای برای حذف پیدا شد
        
        // حالت 1: گره برگ است (بدون فرزند)
        if (node.left == null && node.right == null)
            return null;
        
        // حالت 2: گره تنها یک فرزند دارد
        if (node.left == null)
            return node.right;
        if (node.right == null)
            return node.left;
        
        // حالت 3: گره دو فرزند دارد
        // بزرگ‌ترین گره در سمت چپ را پیدا کنید (Predecessor)
        AVLNode temp = findMax(node.left);
        
        // آی‌دی را جابجا کنید
        node.channelID = temp.channelID;
        
        // گره جایگزین را حذف کنید
        node.left = deleteRec(node.left, temp.channelID);
    }
    
    // اگر درخت خالی شده بود
    if (node == null)
        return null;
    
    // ارتفاع را به‌روزرسانی کنید
    node.height = max(height(node.left), height(node.right)) + 1;
    
    // تعادل را محاسبه کنید
    int balance = getBalance(node);
    
    // چهار حالت دوران (مانند درج)
    
    // حالت LL
    if (balance > 1 && getBalance(node.left) >= 0)
        return rotateRight(node);
    
    // حالت LR
    if (balance > 1 && getBalance(node.left) < 0) {
        node.left = rotateLeft(node.left);
        return rotateRight(node);
    }
    
    // حالت RR
    if (balance < -1 && getBalance(node.right) <= 0)
        return rotateLeft(node);
    
    // حالت RL
    if (balance < -1 && getBalance(node.right) > 0) {
        node.right = rotateRight(node.right);
        return rotateLeft(node);
    }
    
    return node;
}

private AVLNode findMax(AVLNode node) {
    if (node.right == null)
        return node;
    return findMax(node.right);
}
```

**توضیح حذف:**
- **خط 6-8:** اگر گره `null` باشد برگردان `null`
- **خط 17-19:** **حالت 1 (برگ):** گره هیچ فرزندی ندارد، برگردان `null`
- **خط 21-24:** **حالت 2 (یک فرزند):** گره را با فرزندش جایگزین کن
- **خط 26-33:** **حالت 3 (دو فرزند):** بزرگ‌ترین گره از سمت چپ پیدا کن (Predecessor)، داده‌های آن را کپی کن، سپس آن گره جایگزین را حذف کن
- **خط 36-57:** بعد از حذف: ارتفاع و تعادل را دوباره بررسی کنید (ممکن است دوران لازم باشد)

#### ز) عملیات نمایش (Display):

```java
public void display() {
    if (root == null) {
        System.out.println("درخت خالی است");
        return;
    }
    System.out.println("ساختار درخت AVL:");
    displayRec(root, "", true);
}

private void displayRec(AVLNode node, String prefix, boolean isLeft) {
    if (node == null)
        return;
    
    System.out.println(prefix + (isLeft ? "├── " : "└── ") + 
                      "ID: " + node.channelID + " (h=" + node.height + ")");
    
    if (node.left != null || node.right != null) {
        if (node.left != null)
            displayRec(node.left, prefix + (isLeft ? "│   " : "    "), true);
        if (node.right != null)
            displayRec(node.right, prefix + (isLeft ? "│   " : "    "), false);
    }
}
```

**توضیح:**
- **خط 2-4:** اگر درخت خالی باشد، پیام چاپ کن
- **خط 6-7:** سرتیتر و شروع نمایش درخت
- **خط 10-12:** اگر گره `null` باشد، برگردان
- **خط 13-14:** چاپ گره با پیشوند مناسب (`├──` برای چپ، `└──` برای راست)
- **خط 16-20:** اگر فرزند وجود داشته باشد، بازگردان آن‌ها به صورت درختی

---

### 3️⃣ Phase1Main.java - برنامه اصلی

**نقش:** رابط کاربری (CLI) برای تعامل با درخت AVL.

**کد کامل:**

```java
import java.util.Scanner;

public class Phase1Main {
    public static void main(String[] args) {
        AVLTree tree = new AVLTree();
        Scanner sc = new Scanner(System.in);
        
        System.out.println("=== درخت AVL برای کانال‌ها ===");
        System.out.println("دستورات: insert <id> | delete <id> | search <id> | display | exit");
        
        while (sc.hasNext()) {
            String command = sc.next();
            
            if (command.equals("insert")) {
                int id = sc.nextInt();
                tree.insert(id);
                System.out.println("✓ کانال " + id + " اضافه شد");
                
            } else if (command.equals("delete")) {
                int id = sc.nextInt();
                tree.delete(id);
                System.out.println("✓ کانال " + id + " حذف شد");
                
            } else if (command.equals("search")) {
                int id = sc.nextInt();
                if (tree.search(id))
                    System.out.println("✓ کانال " + id + " یافت شد");
                else
                    System.out.println("✗ کانال " + id + " یافت نشد");
                    
            } else if (command.equals("display")) {
                tree.display();
                
            } else if (command.equals("exit")) {
                System.out.println("برنامه پایان یافت");
                break;
            }
        }
        
        sc.close();
    }
}
```

**توضیح:**
- **خط 1:** وارد کردن Scanner برای خواندن ورودی
- **خط 3-6:** ایجاد درخت و Scanner
- **خط 8-9:** چاپ راهنما
- **خط 11:** حلقهٔ اصلی تا وقتی ورودی وجود دارد
- **خط 12:** دستور را بخوانید
- **خط 14-16:** اگر "insert" است، آی‌دی را بخوانید و درج کنید
- **خط 18-20:** اگر "delete" است، آی‌دی را بخوانید و حذف کنید
- **خط 22-27:** اگر "search" است، جستجو کنید و نتیجه را نمایش دهید
- **خط 29-30:** اگر "display" است، درخت را نمایش دهید
- **خط 32-35:** اگر "exit" است، برنامه را پایان دهید
- **خط 38:** Scanner را ببندید

---

## 🧪 مثال‌های عملی و تست

### مثال اول: درج سادهٔ اعداد متوالی

**ورودی:**
```
insert 50
insert 25
insert 75
insert 10
insert 30
insert 60
insert 80
display
exit
```

**توضیح گام به گام:**

1. **insert 50**: درخت خالی است، 50 به عنوان ریشه
   ```
   50
   ```

2. **insert 25**: 25 < 50، درج به سمت چپ
   ```
     50
    /
   25
   ```

3. **insert 75**: 75 > 50، درج به سمت راست
   ```
       50
      /  \
     25   75
   ```

4. **insert 10**: 10 < 50 → سمت چپ، 10 < 25 → سمت چپ
   ```
         50
        /  \
       25   75
      /
     10
   ```

5. **insert 30**: 30 < 50 → سمت چپ، 30 > 25 → سمت راست
   ```
         50
        /  \
       25   75
      / \
     10 30
   ```

6. **insert 60**: 60 > 50 → سمت راست، 60 < 75 → سمت چپ
   ```
         50
        /  \
       25   75
      / \  /
     10 30 60
   ```

7. **insert 80**: 80 > 50 → سمت راست، 80 > 75 → سمت راست
   ```
          50
         /  \
        25   75
       / \  /  \
      10 30 60 80
   ```
   **درخت متوازن است!**

8. **display**:
   ```
   ساختار درخت AVL:
   ├── ID: 50 (h=3)
   │   ├── ID: 25 (h=2)
   │   │   ├── ID: 10 (h=1)
   │   │   └── ID: 30 (h=1)
   │   └── ID: 75 (h=2)
   │       ├── ID: 60 (h=1)
   │       └── ID: 80 (h=1)
   ```

---
## 📊 پیچیدگی الگوریتمی

| عملیات | پیچیدگی زمانی | توضیح |
|--------|---------------|--------|
| درج (Insert) | O(log n) | جستجو + دوران |
| حذف (Delete) | O(log n) | جستجو + دوران |
| جستجو (Search) | O(log n) | جستجوی دودویی |
| نمایش (Display) | O(n) | بازدید تمام گره‌ها |

**فضا:** O(n) برای ذخیرهٔ n گره

---

## 🔑 نکات مهم

1. **تعادل:** میانگین اختلاف ارتفاع فرزندان هر گره حداکثر 1 است
2. **چهار حالت دوران:** LL، RR، LR، RL - هر حالت دارای روند خاصی است
3. **Recursion:** تمام عملیات به صورت بازگشتی انجام می‌شود
4. **بدون کتابخانه:** هیچ TreeMap یا دیگر ساختاری استفاده نشده است
5. **کانال‌ها:** هر کانال یک عدد صحیح مثبت است
6. **Performance:** با استفاده از AVL، جستجو برای 10000 کانال فقط تا 14 مقایسه نیاز دارد
