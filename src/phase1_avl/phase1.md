# فاز اول پروژه ساختمان داده

## مدیریت کانال‌های تأیید شده با AVL Tree

---

## معرفی کلی فاز اول

در فاز اول پروژه EchoVerse، هدف پیاده‌سازی سیستمی برای مدیریت کانال‌های تأیید شده پادکست است.  
هر کانال دارای یک `ChannelID` عددی و یکتا می‌باشد و فقط کانال‌هایی که در سیستم ثبت شده‌اند مجاز به انتشار هستند.

برای انجام این کار، طبق صورت پروژه، **استفاده از درخت جستجوی دودویی خودمتوازن (AVL Tree)** الزامی بوده و استفاده از هرگونه ساختار داده آماده مانند `Array`، `List`، `Set` یا `Map` ممنوع است.

---

## اهداف این فاز

در این فاز سیستم باید بتواند عملیات زیر را انجام دهد:

- افزودن کانال جدید (insert)
- حذف کانال موجود (delete)
- بررسی مجاز بودن یک کانال (search)
- نمایش ساختار درخت به فرم خواسته‌شده در پروژه (display)

---

## ساختار فایل‌ها در فاز اول

فایل‌های مربوط به فاز اول در پوشه زیر قرار دارند:

```

src/phase1_avl/

```

و شامل سه فایل اصلی هستند:

---

### 1️⃣ `AVLNode.java`

این فایل نماینده‌ی **هر نود (رأس)** در درخت AVL است.

#### وظایف این کلاس

- نگهداری مقدار `ChannelID`
- نگهداری ارجاع به فرزند چپ و راست
- نگهداری ارتفاع نود (برای متوازن‌سازی)

#### فیلدهای مهم

- `int channelID`
- `AVLNode left`
- `AVLNode right`
- `int height`

---

### 2️⃣ `AVLTree.java`

این فایل هسته‌ی اصلی فاز اول محسوب می‌شود و تمام منطق درخت AVL در آن پیاده‌سازی شده است.

#### وظایف این کلاس

- مدیریت ریشه‌ی درخت
- پیاده‌سازی عملیات درج، حذف، جستجو
- انجام چرخش‌ها برای حفظ تعادل درخت
- نمایش درخت مطابق خروجی نمونه پروژه

---

### 3️⃣ `Phase1Main.java`

این فایل شامل تابع `main` است و وظیفه‌ی آن:

- دریافت دستورات از ورودی
- صدا زدن متدهای مربوطه در `AVLTree`
- چاپ خروجی نهایی برنامه

این فایل نقش رابط بین کاربر و منطق درخت را ایفا می‌کند.

---

## معرفی ساختمان داده AVL Tree (به زبان ساده)

درخت AVL یک نوع **درخت جستجوی دودویی** است که:

- برای هر نود، اختلاف ارتفاع زیر‌درخت چپ و راست حداکثر ۱ است
- بعد از هر عملیات درج یا حذف، درخت در صورت نیاز **متوازن** می‌شود
- متوازن‌سازی با استفاده از **چرخش‌ها (Rotations)** انجام می‌شود

این ویژگی باعث می‌شود:

- عملیات insert، delete و search همگی در زمان `O(log n)` انجام شوند

---

## توضیح عملیات‌های اصلی

---

### 🔹 درج (Insert)

درج کانال جدید مشابه درخت BST انجام می‌شود:

- اگر مقدار کمتر باشد → به چپ
- اگر مقدار بیشتر باشد → به راست

بعد از درج:

- ارتفاع نودها به‌روزرسانی می‌شود
- در صورت نیاز، چرخش مناسب انجام می‌شود

---

### 🔹 حذف (Delete)

حذف کانال شامل سه حالت است:

1. نود بدون فرزند → حذف مستقیم
2. نود با یک فرزند → جایگزینی با فرزند
3. نود با دو فرزند →  
   استفاده از **predecessor (بزرگ‌ترین نود زیر‌درخت چپ)** برای جایگزینی

پس از حذف:

- ارتفاع نودها اصلاح می‌شود
- تعادل درخت با چرخش‌ها برقرار می‌شود

---

### 🔹 جستجو (Search)

برای بررسی مجاز بودن یک کانال:

- از ریشه شروع می‌کنیم
- مشابه جستجو در BST حرکت می‌کنیم
- در صورت یافتن مقدار، کانال مجاز است

---

### 🔹 نمایش درخت (Display)

نمایش درخت به فرمی انجام می‌شود که در صورت پروژه مشخص شده است:

```

Node LeftChild,RightChild

```

نکات مهم:

- فقط نودهایی که حداقل یک فرزند دارند نمایش داده می‌شوند
- اگر فرزندی وجود نداشته باشد مقدار `-1` چاپ می‌شود
- ترتیب نمایش به صورت بالا به پایین و چپ به راست است

---

## توابع مهم و نقش آن‌ها

| تابع | توضیح |
|----|----|
| `insert()` | افزودن کانال جدید |
| `deleteNode()` | حذف کانال و متوازن‌سازی |
| `search()` | بررسی مجاز بودن کانال |
| `rotateLeft()` | چرخش چپ برای تعادل |
| `rotateRight()` | چرخش راست برای تعادل |
| `getBalance()` | محاسبه اختلاف ارتفاع |
| `displayTree()` | نمایش ساختار درخت |

---

## جمع‌بندی فاز اول

در این فاز:

- یک AVL Tree به صورت کامل و دستی پیاده‌سازی شد
- تمام عملیات خواسته‌شده در پروژه انجام شد
- هیچ ساختار داده آماده‌ای استفاده نشد
- کدها ساده، قابل فهم و قابل دفاع هستند
- فاز اول آماده‌ی تحویل و ورود به فاز دوم می‌باشد

---
