# فاز اول: مدیریت کانال‌های تأیید شده با AVL Tree

## 📌 معرفی فاز اول

در فاز اول پروژه EchoVerse، مسئله‌ای که باید حل کنیم این است:

**مسئله:** پلتفرم EchoVerse هزاران کانال پادکست دارد و باید بتواند در هر لحظه بررسی کند که یک کانال مجاز به انتشار است یا نه. به‌علاوه، باید بتوانیم کانال‌های جدید اضافه کنیم یا کانال‌های موجود را حذف کنیم.
ل
**راه‌حل:** استفاده از یک درخت جستجوی دودویی خودمتوازن به نام **AVL Tree**

چرا AVL Tree؟ زیرا:

- جستجو، افزودن و حذف‌کردن سریع است (زمان `O(log n)`)
- درخت همیشه متوازن است و تمام عملیات‌ها کمتر پیچیده می‌شوند

**قانون مهم:** در این پروژه، استفاده از کتابخانه‌های آماده مانند `ArrayList`، `LinkedList`، `HashSet` یا هر ساختار داده آماده دیگر **ممنوع** است. باید همه چیز را دستی و از صفر پیاده‌سازی کنیم.

---

## 🎯 اهداف و الزامات

### اهداف فاز اول

برنامه‌ی ما باید این کارها را انجام دهد:

1. **درج (Insert):** یک کانال جدید را به درخت اضافه کنیم
2. **حذف (Delete):** یک کانال موجود را حذف کنیم
3. **جستجو (Search):** بررسی کنیم که یک کانال خاص در سیستم موجود است یا نه
4. **نمایش (Display):** ساختار فعلی درخت را نشان دهیم

### الزامات

- هیچ کتابخانه‌ی آماده‌ی ساختمان داده استفاده نشود
- پیاده‌سازی دستی و کامل AVL Tree
- درخت باید خودکار متوازن شود (برنامه خودش انجام دهد)
- کدها ساده و قابل فهم باشند

---

## 📁 ساختار فایل‌ها

فایل‌های فاز اول در پوشه `src/phase1_avl/` قرار دارند:

```text
src/phase1_avl/
    ├── AVLNode.java      (کلاس نود)
    ├── AVLTree.java      (کلاس درخت)
    ├── Phase1Main.java   (برنامه اصلی)
    └── phase1.md         (این فایل)
```

---

## 🧩 شرح تفصیلی هر فایل

### 1️⃣ AVLNode.java - کلاس نود

**نقش:** هر نود (رأس) درخت را نمایندگی می‌کند.

**فیلدهای نود:**

```java
int channelID;        // شناسه کانال
int height;           // ارتفاع نود (برای متوازن‌سازی)
AVLNode left;         // ارجاع به فرزند چپ
AVLNode right;        // ارجاع به فرزند راست
```

**تابع سازنده:**

```java
public AVLNode(int channelID) {
    this.channelID = channelID;
    this.height = 1;       // وقتی درج می‌شود ارتفاع ۱ است
    this.left = null;
    this.right = null;
}
```

**توضیح:** وقتی نود جدیدی درج می‌شود، ارتفاع آن ۱ است (یک نود به تنهایی). بعد از اینکه نود‌های دیگری اضافه شوند، ارتفاع نودهای قدیمی‌تر به‌روزرسانی می‌شود.

---

### 2️⃣ AVLTree.java - کلاس درخت

**نقش:** تمام منطق درخت AVL در این فایل پیاده‌سازی شده است. این فایل بیشترین توابع مهم را دارد.

#### الف) متدهای کمکی

**محاسبه ارتفاع:**

```java
private int height(AVLNode node) {
    if (node == null)
        return 0;
    return node.height;
}
```

**توضیح:** اگر نود `null` باشد (خالی است) ارتفاع برابر ۰ است. وگرنه ارتفاع نود را برمی‌گرداند.

**محاسبه تعادل (Balance):**

```java
private int getBalance(AVLNode node) {
    if (node == null)
        return 0;
    return height(node.left) - height(node.right);
}
```

**توضیح:** در AVL Tree، تعادل یک نود = ارتفاع فرزند چپ - ارتفاع فرزند راست

- اگر `balance = 1` یا `-1` یا `0`: درخت متوازن است (خوب)
- اگر `balance > 1`: درخت بیش‌تر به چپ سنگین است (باید چرخش کنیم)
- اگر `balance < -1`: درخت بیش‌تر به راست سنگین است (باید چرخش کنیم)

#### ب) چرخش‌ها (Rotations)

چرخش‌ها برای متوازن‌سازی درخت استفاده می‌شوند. تصور کنید درخت یطرفه شده است و باید آن را برگردانیم.

**چرخش راست (Right Rotation):**

```java
private AVLNode rotateRight(AVLNode y) {
    AVLNode x = y.left;
    AVLNode t2 = x.right;

    // انجام چرخش
    x.right = y;
    y.left = t2;

    // به‌روزرسانی ارتفاع
    y.height = max(height(y.left), height(y.right)) + 1;
    x.height = max(height(x.left), height(x.right)) + 1;

    return x;  // x نود جدید این قسمت است
}
```

**چرخش چپ (Left Rotation):** مشابه چرخش راست است، اما طرف مقابل.

#### ج) درج (Insert)

```java
public void insert(int channelID) {
    root = insert(root, channelID);
}

private AVLNode insert(AVLNode node, int channelID) {
    // قدم 1: درج مثل BST
    if (node == null)
        return new AVLNode(channelID);

    if (channelID < node.channelID)
        node.left = insert(node.left, channelID);
    else if (channelID > node.channelID)
        node.right = insert(node.right, channelID);
    else
        return node;  // اگر مساوی باشد، درج نمی‌کنیم

    // قدم 2: به‌روزرسانی ارتفاع
    node.height = max(height(node.left), height(node.right)) + 1;

    // قدم 3: بررسی تعادل و چرخش (اگر لازم باشد)
    int balance = getBalance(node);

    // چهار حالت عدم‌تعادل و چرخش‌های مربوطه...
    if (balance > 1 && channelID < node.left.channelID)
        return rotateRight(node);
    // ... و غیره
}
```

**توضیح:** درج مانند درخت جستجویی عادی است، اما بعد‌ازآن:

- ارتفاع به‌روزرسانی می‌شود
- تعادل بررسی می‌شود
- اگر نامتوازن باشد، چرخش انجام می‌شود

#### د) حذف (Delete)

حذف پیچیده‌تر است زیرا سه حالت وجود دارد:

```java
private AVLNode deleteNode(AVLNode node, int key) {
    if (node == null)
        return null;

    // قدم 1: پیدا کردن نود حذف‌شونده
    if (key < node.channelID)
        node.left = deleteNode(node.left, key);
    else if (key > node.channelID)
        node.right = deleteNode(node.right, key);
    else {
        // نود پیدا شد - اکنون حذف می‌کنیم
        
        // حالت ۱: بدون فرزند
        if (node.left == null && node.right == null)
            return null;

        // حالت ۲: فقط یک فرزند
        if (node.left == null)
            return node.right;
        if (node.right == null)
            return node.left;

        // حالت ۳: دو فرزند
        // بزرگ‌ترین نود در زیر‌درخت چپ را پیدا کنیم (predecessor)
        AVLNode temp = getMaxNode(node.left);
        node.channelID = temp.channelID;
        node.left = deleteNode(node.left, temp.channelID);
    }

    // قدم 2: به‌روزرسانی ارتفاع و چرخش (مثل insert)
    node.height = 1 + max(height(node.left), height(node.right));
    int balance = getBalance(node);
    // ... چرخش‌ها
}
```

**توضیح حالت سوم:** اگر نود دو فرزند داشته باشد:

1. بزرگ‌ترین نود در سمت چپ را پیدا می‌کنیم (یا کوچک‌ترین در سمت راست)
2. مقدار آن را جایگزین نود فعلی می‌کنیم
3. آن نود را حذف می‌کنیم (که حالا حداکثر یک فرزند دارد)

#### ه) جستجو (Search)

```java
public boolean search(int channelID) {
    return search(root, channelID);
}

private boolean search(AVLNode node, int channelID) {
    if (node == null)
        return false;

    if (channelID == node.channelID)
        return true;

    if (channelID < node.channelID)
        return search(node.left, channelID);
    else
        return search(node.right, channelID);
}
```

**توضیح:** مثل جستجوی درخت جستجویی عادی است - اگر نود `null` باشد، کانال پیدا نشد

#### و) نمایش (Display)

```java
public void displayTree() {
    displayNode(root);
}

private void displayNode(AVLNode node) {
    if (node == null)
        return;

    // نمایش فقط نودهایی که حداقل یک فرزند دارند
    if (node.left != null || node.right != null) {
        int leftID = (node.left != null) ? node.left.channelID : -1;
        int rightID = (node.right != null) ? node.right.channelID : -1;
        System.out.println(node.channelID + " " + leftID + "," + rightID);
    }

    // بازگشت به فرزندان
    if (node.left != null)
        displayNode(node.left);
    if (node.right != null)
        displayNode(node.right);
}
```

**توضیح:** نمایش درخت به صورت: `NodeID LeftChild,RightChild`

- اگر فرزندی وجود نداشته باشد، مقدار `-1` قرار می‌دهیم
- با بازگشت (recursion) تمام نودها را نمایش می‌دهیم

---

### 3️⃣ Phase1Main.java - برنامه اصلی

**نقش:** رابط بین کاربر و درخت AVL است.

**کار برنامه:**

```java
while (sc.hasNext()) {
    String command = sc.next();

    if (command.equals("insert")) {
        int value = sc.nextInt();
        tree.insert(value);
    }
    else if (command.equals("delete")) {
        int value = sc.nextInt();
        tree.delete(value);
    }
    else if (command.equals("search")) {
        int value = sc.nextInt();
        boolean found = tree.search(value);
        if (found)
            System.out.println("Channel " + value + " is authorized");
        else
            System.out.println("Channel " + value + " is NOT authorized");
    }
    else if (command.equals("display")) {
        tree.displayTree();
    }
}
```

**توضیح:** این برنامه دستورات ورودی را دریافت می‌کند و متدهای مناسب درخت را صدا می‌زند.

---

## ⚙️ چطور کد کار می‌کند - یک مثال

تصور کنید این دستورات را وارد می‌کنیم:

```text
insert 30
insert 20
insert 40
search 20
delete 30
display
```

### مرحله ۱: `insert 30`

- یک نود جدید با مقدار ۳۰ درج می‌شود (ریشه)

```text
30
```

### مرحله ۲: `insert 20`

- ۲۰ < ۳۰، پس به سمت چپ می‌رود

```text
        30
     /
    20
```

### مرحله ۳: `insert 40`

- ۴۰ > ۳۰، پس به سمت راست می‌رود

```text
        30
     /  \
    20  40
```

### مرحله ۴: `search 20`

- شروع از ۳۰: ۲۰ < ۳۰، بروی چپ
- نود ۲۰ پیدا شد
- **خروجی:** `Channel 20 is authorized`

### مرحله ۵: `delete 30`

- نود ۳۰ دو فرزند دارد
- بزرگ‌ترین نود سمت چپ را پیدا می‌کنیم (۲۰)
- مقدار ۲۰ جایگزین ۳۰ می‌شود
- نود ۲۰ اصلی حذف می‌شود

```text
        20
            \
            40
```

### مرحله ۶: `display`

**خروجی:**

```text
20 -1,40
```

(نود ۲۰ فرزند چپ ندارد اما فرزند راست ۴۰ دارد)

---

---

## 📌 خلاصه فاز اول

| بخش | توضیح |
| ------ | -------- |
| **هدف** | ساخت سیستم مدیریت کانال‌های تأیید شده |
| **ابزار** | AVL Tree (درخت جستجویی خودمتوازن) |
| **فایل‌ها** | 3 فایل (Node, Tree, Main) |
| **عملیات** | Insert, Delete, Search, Display |
| **وضعیت** | ✅ تکمیل‌شده و تست‌شده |

---
