# 📊 فاز چهارم: صف انتشار اپیزودها (Min-Heap)

## 🎯 هدف فاز

در این فاز یک **صف اولویت (Priority Queue)** برای مدیریت انتشار اپیزودهای پادکست پیاده‌سازی می‌کنیم.

**ویژگی‌های اصلی:**

- هر اپیزود یک **شناسه (ID)** و **اولویت عددی** دارد
- **عدد کوچکتر** = اولویت بالاتر برای انتشار
- همیشه اپیزودی با **کمترین اولویت** در ریشه Heap است
- عملیات درج و استخراج با پیچیدگی زمانی **O(log n)**

**کاربرد:**
سیستم مدیریت انتشار اپیزودها بر اساس اولویت کاری تیم.

---

## 📁 فایل‌ها و نقش آن‌ها

### 1️⃣ **Episode.java** (نود)

تنها **نگهداری داده** می‌کند:

```java
String id;       // شناسه اپیزود (مثلاً E1، E2)
int priority;    // عدد اولویت (کوچکتر = بالاتر)
```

---

### 2️⃣ **MinHeap.java** (منطق اصلی)

#### **ساختار:** آرایه‌ای از `Episode` با خصوصیات Min-Heap

**رابطه‌های ریشه:**

- فرزند چپ: `2*i + 1`
- فرزند راست: `2*i + 2`
- والد: `(i-1) / 2`

#### **insert(String id, int priority)**

- اپیزود جدید را در انتهای آرایه اضافه کنید
- **Heapify Up**: اپیزود را بالا کشیدن تا شرط Min-Heap حفظ شود
- **پیچیدگی:** O(log n)

```java
// مثال: insert E1 5
// 1. اضافه کنید: [E1(5)]
// 2. والد را بررسی کنید (ندارد)
// 3. تمام شد 
```

#### **extractMin()**

- **ریشه** (کمترین عنصر) را برگردانید
- **ریشه جدید:** آخرین عنصر آرایه
- **Heapify Down**: ریشه را پایین کشیدن تا شرط Min-Heap حفظ شود
- **پیچیدگی:** O(log n)

```java
// مثال: extractMin() از [E4(1), E2(2), E5(3), E1(5), E3(8)]
// 1. برگردان: E4(1)
// 2. ریشه جدید: E3(8)
// 3. پایین کشیدن: [E2(2), E1(5), E5(3), E3(8)]
```

#### **delete(String id)**

- اپیزود خاص را بر اساس ID پیدا کنید
- جایگزین کنید با **آخرین عنصر**
- **Heapify Down** انجام دهید
- **پیچیدگی:** O(n)

#### **display()**

- نمایش اپیزودها به ترتیب اولویت (از کمترین به بیشترین)
- فرمت: `ID(Priority) ID(Priority) ...`
- خروجی: `E4(1) E2(2) E5(3) E1(5) E3(8)`

#### **heapSort()**

- تمام اپیزودها را **بیرون کشیدن (extractMin)** و در آرایه جدید ذخیره کنید
- نتیجه: اپیزودها به ترتیب اولویت صعودی مرتب‌شده‌اند
- **پیچیدگی:** O(n log n)

```java
// مثال: heapSort() 
// extractMin() → [E4(1), E2(2), E5(3), E1(5), E3(8)]
// خروجی: Sorted episodes by priority:
//        E4(1) E2(2) E5(3) E1(5) E3(8)
```

---

### 3️⃣ **Phase4Main.java** (رابط CLI)

دستورات پشتیبانی‌شده:

```bash
insert <id> <priority>    # اضافه کردن اپیزود
display                   # نمایش Heap
extract-min              # استخراج و حذف Min
delete <id>              # حذف اپیزود خاص
heap-sort                # مرتب‌سازی و نمایش
```

---

## 🔄 مثال جزء‌به‌جزء

### ورودی

```text
insert E1 5
insert E2 2
insert E3 8
insert E4 1
insert E5 3
display
extract-min
display
delete E3
display
heap-sort
```

### خروجی

```text
E4(1) E2(2) E5(3) E1(5) E3(8)

Extracted episode: E4 with priority 1

E2(2) E5(3) E1(5) E3(8)

Episode E3 deleted

E2(2) E5(3) E1(5)

Sorted episodes by priority:
E4(1) E2(2) E5(3) E1(5) E3(8)
```

### توضیح مرحله‌به‌مرحله

**درج 5 اپیزود:**

```text
      E4(1)
      /    \
   E2(2)   E5(3)
   /  \
E1(5) E3(8)
```

**بعد از extractMin (حذف E4):**

```text
      E2(2)
      /    \
   E5(3)   E1(5)
   /
E3(8)
```

**بعد از delete E3:**

```text
      E2(2)
      /    \
   E5(3)   E1(5)
```

**heapSort:**

- E4(1) → E2(2) → E5(3) → E1(5) → E3(8) ✓

---

## 📋 ورودی/خروجی فرمت

### نمونه خروجی

```text
E4(1) E2(2) E5(3) E1(5) E3(8)
Extracted episode: E4 with priority 1
E2(2) E5(3) E1(5) E3(8)
Episode E3 deleted
E2(2) E5(3) E1(5)
Sorted episodes by priority:
E4(1) E2(2) E5(3) E1(5) E3(8)
```

---

## ⚡ نکات و محدودیت‌ها

| نکته | توضیح |
| ------ | -------- |
| **اپیزودهای یکتا** | هر اپیزود یک ID منحصربه‌فرد دارد |
| **بدون تکرار** | یک ID نمی‌تواند دوبار اضافه شود |
| **حفظ ویژگی Heap** | در هر عملیات (درج، حذف، استخراج)، ساختار Min-Heap همیشه معتبر باقی می‌ماند |
| **درج سریع** | افزودن اپیزود جدید با پیچیدگی O(log n) |
| **O(n) حذف** | حذف نیاز به جستجو دارد |

---

## 🚀 نحوه اجرا

### 1️⃣ ورود به پوشه `src`

```bash
cd src
```

### 2️⃣ کامپایل

```bash
javac phase4_heap/*.java
```

### 3️⃣ اجرای تعاملی

```bash
java phase4_heap.Phase4Main
```

سپس دستورات را یکی یکی وارد کنید و نتایج را مشاهده کنید.

**مثال:**

```text
insert E1 5
insert E2 2
display
extract-min
heap-sort
```

---
